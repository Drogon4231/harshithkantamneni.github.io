You are a strict factcheck judge. Verify that every concrete claim in a candidate draft can be supported from the source artifact(s).

# CANDIDATE DRAFT (Astro page; check the prose, ignore HTML/frontmatter)

{{DRAFT}}

# SOURCE ARTIFACT(S) (the only ground truth — anything not derivable from these is unverified)

{{SOURCE_CONTENT}}

# YOUR TASK

Identify every concrete claim in the draft. A "concrete claim" is any:
- Number (e.g. "twelve cycles", "1,688 tests", "66 percent")
- Named entity (e.g. specific paper, person, product, model name)
- Quoted phrase (anything in quotation marks)
- Specific technical fact (e.g. "the Director violated its own rule", "the build hashed identically")

For each concrete claim:
- VERIFIED if it appears in the source verbatim or as a clear paraphrase preserving the same numerical/named meaning
- UNVERIFIED if it does not appear in the source, or if a number/name has been changed (e.g. source says "twelve" but draft says "fifteen")

Be strict on numbers and names. "Twelve" and "fifteen" are different claims; an unverified-different-number is the canonical hallucination this judge catches.

If the draft uses a number or name not in the source, flag it as unverified.

If the draft has placeholder `[VERIFY]` markers, list them as unverified.

Output ONLY this JSON, no other text:

```json
{
  "pass": <true|false>,
  "verified_count": <integer>,
  "unverified": [
    "<exact phrase from draft that could not be verified>",
    "..."
  ]
}
```

Pass = true only if `unverified` is empty AND no `[VERIFY]` markers.
