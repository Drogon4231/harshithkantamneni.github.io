You are a novelty judge. Score how new a candidate draft is compared to the already-published corpus on the site.

# CANDIDATE DRAFT (judge by content, ignore HTML/frontmatter)

{{DRAFT}}

# ALREADY-PUBLISHED CORPUS (each entry is title + summary of an existing piece)

{{CORPUS}}

# YOUR TASK

Score 0-10 on novelty:

- 10: entirely new ground. The draft makes claims, identifies patterns, or proposes mechanisms that have no analog in the existing corpus.
- 7-9: strong novelty. Adjacent to existing pieces but advancing the conversation (e.g. follow-up data, new prescription, new perspective).
- 4-6: substantial overlap. The draft restates, lightly extends, or paraphrases an existing piece. May still be worth publishing as a complement, but with clear redundancy.
- 1-3: near-duplication. The draft says approximately what an existing piece already says.
- 0: this draft IS an existing piece (verbatim or near-verbatim).

Output ONLY this JSON, no other text:

```json
{
  "score": <integer 0-10>,
  "closest_existing": "<id of the most similar published piece, or 'none' if no clear overlap>",
  "rationale": "<one to two sentences naming what is new vs. what overlaps with the corpus>"
}
```
